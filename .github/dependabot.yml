# Keeps the actions, base image and qbittorrent-api up to date.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule: { interval: monthly }
  - package-ecosystem: docker
    directory: /
    schedule: { interval: monthly }
  - package-ecosystem: pip
    directory: /
    schedule: { interval: monthly }
